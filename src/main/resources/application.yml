spring:
  datasource:
    url: jdbc:mysql://localhost:3306/duntalk?serverTimezone=Asia/Seoul
    username: root
    password: 1004
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

neople:
  api-key: ${NEOPLE_API_KEY}